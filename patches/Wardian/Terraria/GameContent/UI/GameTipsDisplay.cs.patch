--- decompiled/TerrariaUnified/Terraria/GameContent/UI/GameTipsDisplay.cs
+++ decompiled/Wardian/Terraria/GameContent/UI/GameTipsDisplay.cs
@@ -177,7 +_,7 @@
 		if (_lastTip != null) {
 			list.Remove(_lastTip);
 		}
-		string key = (_lastTip = ((list.Count != 0) ? list[Main.rand.Next(list.Count)] : LocalizedText.Empty)).Key;
+		string key = (_lastTip = ((list.Count != 0) ? list[Main.rand.Next(list.Count)] : LocalizedText.EMPTY)).Key;
 		if (Language.Exists(key)) {
 			textKey = key;
 		}
